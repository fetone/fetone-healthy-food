<view>当前所在的城市</view>
<button type="primary" class="pos-btn" bindtap="posCurrentCity">定位到当前城市</button>
<view class="hot-city">热门城市</view>
<view class="hot-city-list">
  <view class="city-name" wx:for="{{hotCitys}}" wx:key="index" bindtap="posHotCity" data-city="{{item}}">{{item}}</view>
</view>

